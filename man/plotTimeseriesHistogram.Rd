% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotTimeseriesHistogram.R
\name{plotTimeseriesHistogram}
\alias{plotTimeseriesHistogram}
\title{Plot binned/aggregated continuous data as a histogram}
\usage{
plotTimeseriesHistogram(
  timeseries_id,
  startDay,
  endDay,
  bin_width = 1,
  bin_width_units = "days",
  years = NULL,
  transformation = "sum",
  threshold = 0,
  completeness_label = FALSE,
  title = TRUE,
  custom_title = NULL,
  lang = "en",
  tzone = "UTC",
  data = FALSE,
  con = NULL
)
}
\arguments{
\item{timeseries_id}{Timeseries ID from \code{continuous.timeseries}.}

\item{startDay}{Start day-of-year selector (1-366, Date, or \code{"yyyy-mm-dd"}).}

\item{endDay}{End day-of-year selector (1-366, Date, or \code{"yyyy-mm-dd"}).}

\item{bin_width}{Numeric width for each bin (e.g., \code{1}, \code{6}, \code{24}).}

\item{bin_width_units}{Bin units. One of \code{"hours"}, \code{"days"}, \code{"weeks"}, \code{"months"}, or \code{"years"}.}

\item{years}{Optional numeric vector of calendar years to include
(e.g., \code{c(2020, 2021, 2024)}). If the \code{startDay}/\code{endDay} range crosses
Dec 31 -> Jan 1, specify the December year(s). If \code{NULL}, defaults to the
current year.}

\item{transformation}{Aggregation applied within each bin. One of \code{"sum"}, \code{"mean"}, \code{"min"}, \code{"max"}, \code{"median"}, or \code{"integral"}. \code{"integral"} computes \code{sum(value * duration_seconds)} and is useful for rate→total use cases (e.g., exported volume from flow).}

\item{threshold}{A number between 0 and 1 indicating the minimum proportion of non-missing values required in a bin for it to be included in the plot. Default is \code{0} (include all bins regardless of missingness).}

\item{completeness_label}{Should completeness information be included above each bar? Default is \code{FALSE}.}

\item{title}{Should a plot title be shown? Default is \code{TRUE}.}

\item{custom_title}{Optional custom title.}

\item{lang}{Language for metadata labels in title/axes (\code{"en"} or \code{"fr"}).}

\item{tzone}{Timezone for binning and axis display. Default is \code{"UTC"}.}

\item{data}{If \code{TRUE}, return plotting data with the plot.}

\item{con}{A connection to the AquaCache database. \code{NULL} uses
\code{\link[=AquaConnect]{AquaConnect()}} and automatically disconnects.}
}
\value{
A plotly object, or a list with \code{plot} and \code{data} when \code{data = TRUE}.
}
\description{
Plot a histogram-style bar chart for a continuous AquaCache timeseries using corrected values. Binning and aggregation are pushed to PostgreSQL where possible for improved performance on high-frequency data.
}
