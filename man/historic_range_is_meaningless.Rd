% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{historic_range_is_meaningless}
\alias{historic_range_is_meaningless}
\title{Check if historic range is meaningless for given aggregation types and resolution
Determines if historic ranges should be considered meaningless based on the aggregation types, resolution, and record rate. Specifically, if the resolution is sub-daily (hourly or maximum with sub-daily record rate) and the aggregation type is "sum", then historic ranges are considered meaningless.}
\usage{
historic_range_is_meaningless(
  aggregation_types,
  resolution,
  record_rate_seconds = NULL
)
}
\arguments{
\item{aggregation_types}{A character vector of aggregation types (e.g., "sum", "min", "max").}

\item{resolution}{A character string indicating the resolution (e.g., "hour", "day", "max").}

\item{record_rate_seconds}{Optional numeric value indicating the record rate in seconds, used to determine if "max" resolution is sub-daily.}
}
\value{
A logical value indicating whether the historic range is considered meaningless.
}
\description{
Check if historic range is meaningless for given aggregation types and resolution
Determines if historic ranges should be considered meaningless based on the aggregation types, resolution, and record rate. Specifically, if the resolution is sub-daily (hourly or maximum with sub-daily record rate) and the aggregation type is "sum", then historic ranges are considered meaningless.
}
