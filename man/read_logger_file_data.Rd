% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logger_file_data.R
\name{read_logger_file_data}
\alias{read_logger_file_data}
\title{Read logger files to tabular continuous data}
\usage{
read_logger_file_data(file, file_type = NULL, default_tz = "UTC")
}
\arguments{
\item{file}{Full path to a \code{.xle}, \code{.html}, or \code{.hobo} logger file.}

\item{file_type}{Optional file extension override. This is useful for Shiny
uploads where the temporary \code{datapath} may not keep the original extension.}

\item{default_tz}{Time zone used when the logger file does not include an
explicit UTC offset. Use either a single R time zone name from
\code{OlsonNames()} (for example, \code{"America/Whitehorse"} or \code{"UTC"}) or a fixed
UTC offset in hours (for example, \code{-7} for UTC-07:00). Fixed offsets may
also be supplied as strings like \code{"UTC-07:00"}, \code{"-07:00"}, or \code{"GMT-07:00"}.
Defaults to UTC.}
}
\value{
A data frame with \code{datetime} in UTC and one column per logger
measurement.
}
\description{
Converts Solinst \code{.xle}, In-Situ/VuSitu \code{.html}, and supported Onset HOBO
\code{.hobo} logger files to a tabular data frame for continuous data upload. The
returned \code{datetime} column is converted to UTC. This function does not upload,
move, copy, or track files.
}
